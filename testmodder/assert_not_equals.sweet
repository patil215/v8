syntax assertNotEquals = function (ctx) {
    let paramCtx = ctx.contextify(ctx.next().value);
    
    let left = paramCtx.expand('expr').value;
    paramCtx.next();
    let right = paramCtx.expand('expr').value;

    return #`assertNotEqualsF(() => ${left}, () => ${right})`;
};
